# Build
/build/
/build-*/
CMakeFiles/
CMakeCache.txt
cmake_install.cmake
Makefile
compile_commands.json
DEV_services
# Logs
*.log
*.trace
*.out
.cache
# IDE
.vscode/
.idea/
*.swp

# AI tools — per-machine state, not for the repo
.claude/
.codex/
.aider*
.cursor/
.copilot/

# OS
.DS_Store
Thumbs.db
_codeql_detected_source_root

# Secrets / keys
*.key
*.pem
*.crt
*.p12
*.pfx
*.der
*.cer
*.jwk
*.jwe
*.jwt
*.kdbx
*.keystore
*.keytab
*.sk
*.pk
id_ed25519
id_rsa
authorized_keys

# Local configs
config/*.local.json
config/*.secrets.json
config/*.private.json

# BaseFWX dependency checkout
basefwx/

# Local notes for AI agents — kept out of git deliberately.
# [local-only development file removed] documents personal infra (build-host details, home paths), so it
# stays local-only rather than in this public repo.
[local-only development file removed]
[local-only development file removed]

# Python
__pycache__/
*.pyc

# Archives
*.zip
*.tar
*.tgz
*.txz
vendor/

# Debian / dpkg build outputs (cpack, dpkg-buildpackage leftovers)
*.deb
*.ddeb
*.buildinfo
*.changes
*.dsc
*.tar.xz
!third_party/prebuilt/yume-vendor-prebuilt.tar.xz
*.tar.gz
*.tar.zst
*-dbgsym_*

# Cloudflare Wrangler dev cache
.wrangler/

# Build output artifacts (binaries, .app, .dmg) — never commit
bins/
